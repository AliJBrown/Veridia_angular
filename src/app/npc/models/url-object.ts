export interface UrlObject{

    /**
     * string to hold first name url
     */
    fURL:string;

    /**
     * string to hold last name url
     */
    lURL:string;

    /**
     * random number for first name (included here because max will
     * be different for each race)
     */
    fnum:number;

    /**
     * random number for last name (included here because max will
     * be different for each race )
     */
    lnum:number;

}